<!DOCTYPE html>
<html lang="bg">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Книжарница</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <header>
    <nav>
      <ul>
        <li><a href="/">Начало</a></li>
        <li><a href="/books">Книги</a></li>
        <li><a href="/authors">Автори</a></li>
        <li><a href="/publishers">Потребители</a></li>

        <!-- Display dashboard/logout links or login form based on user role -->
        <% if (session.admin) { %>
          <!-- Admin is logged in -->
          <li><a href="/admin/dashboard">Администраторски Панел</a></li>
          <li><a href="/logout">Изход</a></li>
        <% } else if (session.publisher) { %>
          <!-- Publisher is logged in -->
          <li><a href="/publishers/dashboard">Потребителски Панел</a></li>
          <li><a href="/logout">Изход</a></li>
        <% } else { %>
          <!-- No one is logged in: Show single login form -->
          <li>
            <form action="/login" method="POST" style="display: inline;">
              <input type="text" name="username" placeholder="Потребителско име" required>
              <input type="password" name="password" placeholder="Парола" required>
              <button type="submit">Вход</button>
            </form>
          </li>
        <% } %>
      </ul>
    </nav>
  </header>